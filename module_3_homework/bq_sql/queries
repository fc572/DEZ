-- Creating external table referring to gcs path
CREATE OR REPLACE EXTERNAL TABLE `dez_module_one_fc_demo_dataset.ny_yellow_taxi_2024`
OPTIONS (
  format = 'parquet',
  uris = ['gs://dez_module_one_fc_demo_terra_bucket/yellow_tripdata_2024-*.parquet']
);


CREATE OR REPLACE TABLE `dez_module_one_fc_demo_dataset.ny_yellow_taxi_2024_regular` AS
SELECT * FROM `dez_module_one_fc_demo_dataset.ny_yellow_taxi_2024`;

-- What is count of records for the 2024 Yellow Taxi Data?
select count(*) from `dez_module_one_fc_demo_dataset.ny_yellow_taxi_2024`

select count(*) from `dez_module_one_fc_demo_dataset.ny_yellow_taxi_2024_regular`

-- Write a query to count the distinct number of PULocationIDs for the entire dataset on both the tables.
select DISTINCT(PULocationID) from `dez_module_one_fc_demo_dataset.ny_yellow_taxi_2024`

select DISTINCT(PULocationID) from `dez_module_one_fc_demo_dataset.ny_yellow_taxi_2024_regular`

-- Write a query to retrieve the PULocationID from the table (not the external table) in BigQuery. 
-- Now write a query to retrieve the PULocationID and DOLocationID on the same table. 
-- Why are the estimated number of Bytes different?
select PULocationID from `dez_module_one_fc_demo_dataset.ny_yellow_taxi_2024_regular`
select PULocationID, DOLocationID from `dez_module_one_fc_demo_dataset.ny_yellow_taxi_2024_regular`

-- How many records have a fare_amount of 0?
select count(*) from `dez_module_one_fc_demo_dataset.ny_yellow_taxi_2024_regular`
where fare_amount = 0;

-- What is the best strategy to make an optimized table in Big Query if your query will always 
-- filter based on tpep_dropoff_datetime and order the results by VendorID (Create a new table with this strategy)

-- Partition by tpep_dropoff_datetime and Cluster on VendorID
CREATE OR REPLACE TABLE dez_module_one_fc_demo_dataset.ny_yellow_taxi_2024_partitioned_clustered
PARTITION BY DATE(tpep_dropoff_datetime)
CLUSTER BY VendorID AS
SELECT * FROM dez_module_one_fc_demo_dataset.ny_yellow_taxi_2024_regular;



--  Write a query to retrieve the distinct VendorIDs between tpep_dropoff_datetime 2024-03-01 and 2024-03-15 
select DISTINCT(VendorId) from `dez_module_one_fc_demo_dataset.ny_yellow_taxi_2024_regular`
where tpep_dropoff_datetime BETWEEN '2024-03-01' AND '2024-03-15' 

select DISTINCT(VendorId) from `dez_module_one_fc_demo_dataset.ny_yellow_taxi_2024_partitioned_clustered`
where tpep_dropoff_datetime BETWEEN '2024-03-01' AND '2024-03-15' 


select count(*) from dez_module_one_fc_demo_dataset.ny_yellow_taxi_2024_regular